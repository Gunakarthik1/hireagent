services:
  - type: web
    name: hireagent
    runtime: python
    buildCommand: pip install -e . && cd dashboard && npm install && npm run build
    startCommand: uvicorn src.hireagent.api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
